# URS-First Ingestion + Sprint Planning Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add URS-first ingestion mode + sprint timeline planning to the AI Software Factory so a published URS can be dropped in and broken down into a prioritized backlog and sprint plan automatically. Build on `main`.

**Architecture:** Three stages, all token-conscious for very large URS (400+ pages):

1. **Compile (deterministic, zero-LLM)** — `/foundation:urs` parses `urs/main.md` → `urs/index.json` (full requirement text on disk; consumers slice by ID, never load whole) + new `urs/applies-to.json` (NFR/UR/VR → FR map, precomputed once).
2. **Cluster + sprint plan (one LLM pass on compact JSON)** — new `/foundation:sprint-plan` reads `index.json` + `applies-to.json`, derives complexity points per FR (`tables_touched + applies_to_count + risk_zone + dep_count`), builds dep DAG, emits `urs/clusters.json` + `urs/sprint-plan.md` with Sprint 0 walking skeleton and ordered delivery waves.
3. **Lazy task expansion (per FR, just-in-time)** — `/foundation:shape-spec --from-urs FR-XX` writes `urs/tasks/FR-XX.json` with the spec/migration/RPC/action/component/tests breakdown for that one FR. Never explodes all FRs eagerly.

A new `/foundation:plan --from-urs` mode skips the discover-derived feature list and treats `urs/index.json` FR rows as the canonical backlog source.

Subagent fan-out for clustering is **deferred** — interface ready (single-cluster v1), wire fan-out only when a 200+ FR URS proves it necessary.

**Tech Stack:** Markdown command specs (Claude Code slash commands), JSON artifacts, no runtime code. Existing factory: Next.js 16 + Supabase template, `.claude/` directory of commands and standards docs.

**Branch:** `main` (this plan executes on main; URS plumbing baseline is cherry-picked from `xlsmart-coaching`).

---

## File Structure

### Cherry-picked from `xlsmart-coaching` (Phase 1)

- `.claude/docs/adr/0001-urs-project-state-separation.md` — ADR locking the URS / project-state.md separation
- `.claude/docs/adr/0002-urs-source-format.md` — ADR locking Markdown→LaTeX URS format
- `.claude/commands/foundation/urs-draft.md` — `/foundation:urs:draft` command (interactive URS authoring)
- `.claude/commands/foundation/urs.md` — `/foundation:urs` compile/reconcile command
- `latex_template/main.tex` — formal LaTeX layout template

### Created on `main`

- `.claude/docs/adr/0003-urs-first-ingestion-and-sprint-planning.md` — locks the four design decisions (sprint sizing, lazy expansion, full-text index, deferred fan-out)
- `.claude/commands/foundation/sprint-plan.md` — `/foundation:sprint-plan` command

### Modified on `main`

- `.gitignore` — add `urs/*` LaTeX build artifacts + `supabase/snippets/`
- `.claude/commands/foundation/urs.md` — add Step 4.5 emitting `urs/applies-to.json`
- `.claude/commands/foundation/plan.md` — add Step 0 `--from-urs` mode (bypass discover, treat URS as canonical)
- `.claude/commands/foundation/shape-spec.md` — extend Step 0 to write `urs/tasks/FR-XX.json` on `--from-urs`
- `.claude/commands/foundation/validate.md` — add URS-first artifact checks
- `.claude/CLAUDE.md` — add URS-first workflow, new commands in table, ADR 0003 reference
- `.claude/docs/workflows.md` — add URS-first development workflow section
- `README.md` — add URS-first phase sequence and link to sprint planning
- `.claude/docs/standards-index.yml` — register the three ADRs

### Generated by commands at runtime (downstream artifacts, not part of this plan)

- `urs/index.json` (already part of `/foundation:urs`)
- `urs/applies-to.json` (new — Phase 2)
- `urs/clusters.json` (new — Phase 4)
- `urs/sprint-plan.md` (new — Phase 4)
- `urs/tasks/FR-XX.json` (new — Phase 5)

---

## Phase 1 — Bring URS Plumbing Baseline to `main`

The URS plumbing was developed on the `xlsmart-coaching` workstream and proven against the SIM Registration Portal demo. It is now stable enough to ship to `main` as factory baseline. The five tasks in this phase pull only the **factory** commits (ADRs, commands, template) — never the demo data (SIMREG-specific `urs/main.md`, `urs/index.json`).

### Task 1: Update .gitignore for URS build artifacts

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: Append URS LaTeX build artifact patterns and Supabase scratch**

Open `.gitignore` and append after the existing `latex_template/*.toc` line:

```gitignore
urs/*.aux
urs/*.fdb_latexmk
urs/*.fls
urs/*.log
urs/*.pdf
urs/*.synctex.gz
urs/*.toc

# Supabase Studio scratch SQL (not source)
supabase/snippets/
```

- [ ] **Step 2: Verify**

Run: `tail -15 .gitignore`
Expected: shows the appended block.

- [ ] **Step 3: Commit**

```bash
git add .gitignore
git commit -m "chore: ignore urs/ LaTeX artifacts and supabase/snippets/"
```

### Task 2: Cherry-pick ADR 0001 + 0002 (URS architecture decisions)

**Files:**
- Create: `.claude/docs/adr/0001-urs-project-state-separation.md` (via cherry-pick)
- Create: `.claude/docs/adr/0002-urs-source-format.md` (via cherry-pick)

- [ ] **Step 1: Confirm the source commit exists**

Run: `git log --oneline xlsmart-coaching | grep b8c805a`
Expected: `b8c805a docs: ADR 0001 + 0002 for URS architecture`

- [ ] **Step 2: Cherry-pick**

```bash
git cherry-pick b8c805a
```

Expected: clean apply. If conflicts (none expected — files don't exist on main), abort with `git cherry-pick --abort` and ask user.

- [ ] **Step 3: Verify**

Run: `ls .claude/docs/adr/`
Expected: `0001-urs-project-state-separation.md  0002-urs-source-format.md`

- [ ] **Step 4: No commit needed**

Cherry-pick already committed.

### Task 3: Cherry-pick `/foundation:urs:draft` command

**Files:**
- Create: `.claude/commands/foundation/urs-draft.md` (via cherry-pick)

- [ ] **Step 1: Cherry-pick**

```bash
git cherry-pick 2bc1ce0
```

Expected: clean apply.

- [ ] **Step 2: Verify**

Run: `ls .claude/commands/foundation/urs-draft.md && head -5 .claude/commands/foundation/urs-draft.md`
Expected: file exists, starts with `# /foundation:urs:draft`.

- [ ] **Step 3: No commit needed**

### Task 4: Cherry-pick `/foundation:urs` compile command

**Files:**
- Create: `.claude/commands/foundation/urs.md` (via cherry-pick)

- [ ] **Step 1: Cherry-pick**

```bash
git cherry-pick bde797d
```

Expected: clean apply.

- [ ] **Step 2: Verify**

Run: `ls .claude/commands/foundation/urs.md && grep -c '^## Step' .claude/commands/foundation/urs.md`
Expected: file exists; output `≥ 7` (at least 7 step headings).

- [ ] **Step 3: No commit needed**

### Task 5: Bring `latex_template/main.tex` to main

`latex_template/main.tex` was added on `xlsmart-coaching` in commit `c2afd93`, but that commit also contains demo-only changes. Copy the single file directly instead of cherry-picking the whole commit.

**Files:**
- Create: `latex_template/main.tex`

- [ ] **Step 1: Extract the file from xlsmart-coaching**

```bash
git show xlsmart-coaching:latex_template/main.tex > latex_template/main.tex
```

- [ ] **Step 2: Verify**

Run: `wc -l latex_template/main.tex && head -5 latex_template/main.tex`
Expected: file is non-empty; first line is a LaTeX preamble (`\documentclass...` or similar).

- [ ] **Step 3: Commit**

```bash
git add latex_template/main.tex
git commit -m "feat(foundation): add LaTeX template for URS compilation"
```

---

## Phase 2 — Harden `/foundation:urs` for Downstream Consumption

`/foundation:urs` already emits `index.json` (FR/NFR/UR/VR rows). Add a precomputed map so downstream commands don't have to re-derive which NFR/UR/VR applies to which FR.

### Task 6: Emit `urs/applies-to.json` from `/foundation:urs`

`applies-to.json` is the precomputed answer to "for FR-XX, which NFR/UR/VR rows constrain it?" Derived once during compile, consumed by `/foundation:sprint-plan` (complexity weighting) and `/foundation:shape-spec --from-urs` (constraints injected into spec).

**Derivation rule (deterministic):** for each NFR/UR/VR row, scan its `text` for FR-id mentions (`FR-01`, `FR-04`, etc.). Each FR mentioned receives a back-reference to that NFR/UR/VR. If a NFR/UR/VR row mentions no FR, mark it `applies_to: ["*"]` (applies to all FRs in scope) and emit a warning.

**Files:**
- Modify: `.claude/commands/foundation/urs.md` — insert new section between Step 4 and Step 5

- [ ] **Step 1: Read the current Step 4 / Step 5 boundary**

Run: `grep -n '^## Step' .claude/commands/foundation/urs.md`
Expected: lists current step headers. Note the line just before `## Step 5 — Reconcile`.

- [ ] **Step 2: Insert Step 4.5 — Emit `applies-to.json`**

Add the following section in `.claude/commands/foundation/urs.md` immediately before `## Step 5 — Reconcile project-state.md`:

```markdown
## Step 4.5 — Emit `urs/applies-to.json`

Precompute the cross-cutting map from non-functional / user-role / validation
requirements to the functional requirements they constrain. Downstream
commands consume this map directly — no LLM re-derivation per call.

For every requirement with class in {NFR, UR, VR}:

1. Scan its `text` field for substrings matching the regex
   `\bFR-\d{2,3}\b` (case-sensitive). Collect unique matches.
2. If at least one FR id is found, set `applies_to` to that list.
3. If no FR id is found, set `applies_to: ["*"]` and emit a warning in the
   compile report: `NFR-/UR-/VR-XX has no explicit FR scope — applied to
   all FRs by default.`

Also build the inverse map keyed by FR id: for each FR, list every
NFR/UR/VR id whose `applies_to` includes that FR (or `"*"`).

Write `urs/applies-to.json`:

```json
{
  "compiled_at": "<ISO 8601 timestamp matching index.json>",
  "constraints": [
    {
      "id": "NFR-01",
      "class": "NFR",
      "applies_to": ["FR-01", "FR-08"]
    },
    {
      "id": "VR-01",
      "class": "VR",
      "applies_to": ["FR-05", "FR-06"]
    }
  ],
  "by_fr": {
    "FR-01": ["NFR-01", "UR-01"],
    "FR-05": ["VR-01", "UR-02"],
    "FR-06": ["VR-01", "UR-02"]
  }
}
```

**Consumers:**
- `/foundation:sprint-plan` reads `applies_to_count` per FR for complexity scoring.
- `/foundation:shape-spec --from-urs FR-XX` reads `by_fr["FR-XX"]` and injects the listed constraints into the spec front-matter.
- `/foundation:validate` cross-checks that every constraint's `applies_to` references a real FR.
```

- [ ] **Step 3: Update Step 6 (Drift Report) to mention the new file**

Locate the `Files written:` block inside Step 6 of `urs.md` and edit it from:

```
Files written:
  urs/main.tex
  urs/index.json
  .claude/docs/project-state.md (modified)
```

to:

```
Files written:
  urs/main.tex
  urs/index.json
  urs/applies-to.json
  .claude/docs/project-state.md (modified)
```

- [ ] **Step 4: Update the trailing `FILES_CREATED:` machine line**

In the closing block of `urs.md`, change:

```
FILES_CREATED: urs/main.tex, urs/index.json
```

to:

```
FILES_CREATED: urs/main.tex, urs/index.json, urs/applies-to.json
```

- [ ] **Step 5: Verify**

Run: `grep -c '^## Step 4.5' .claude/commands/foundation/urs.md && grep -c 'applies-to.json' .claude/commands/foundation/urs.md`
Expected: first count = `1`, second count `≥ 4`.

- [ ] **Step 6: Commit**

```bash
git add .claude/commands/foundation/urs.md
git commit -m "feat(foundation): emit applies-to.json from /foundation:urs"
```

---

## Phase 3 — URS-First Plan Mode

`/foundation:plan` today derives features from `product-mission.md` (output of `/foundation:discover`). Add a mode that treats a published URS as the canonical source: skip discover, ingest `urs/index.json` directly, write the backlog from FR rows.

### Task 7: Add `--from-urs` mode to `/foundation:plan`

**Files:**
- Modify: `.claude/commands/foundation/plan.md`

- [ ] **Step 1: Read current preconditions and Step 1**

Run: `head -30 .claude/commands/foundation/plan.md`

- [ ] **Step 2: Add usage section**

Insert immediately after the opening paragraph (before `Output: ...`):

```markdown
**Usage:**

- `/foundation:plan` — derive backlog from `product-mission.md` (default; runs after `/foundation:discover`).
- `/foundation:plan --from-urs` — treat a published URS as the canonical source. Skips discover-derived feature derivation; ingests `urs/index.json` directly. Use when the project starts from a finished URS rather than building one in parallel.

```

- [ ] **Step 3: Adjust preconditions**

Replace the existing preconditions block with:

```markdown
**Preconditions:**

- `.claude/project-config.json` must exist (run `/foundation:init`)
- `project-config.json` status should be `"active"`
- **Default mode:** `.claude/docs/foundation/product-mission.md` must be completed (run `/foundation:discover`)
- **`--from-urs` mode:** `urs/index.json` must exist (run `/foundation:urs`). `product-mission.md` may be a stub.
```

- [ ] **Step 4: Insert Step 0 — Mode Selection**

Insert before the existing `## Step 1 — Read Context`:

```markdown
## Step 0 — Mode Selection

If invoked with `--from-urs`:

1. Verify `urs/index.json` exists. If not, abort: "URS index missing. Run `/foundation:urs` first to compile the URS source."
2. Set mode = `urs-first`. Skip Step 2 (Derive Features). The feature list comes from `urs/index.json` FR rows directly. Step 3 (Identify Dependencies) and onward still apply.
3. Skip the question "Are there any features not in the use cases?" — in `urs-first` mode the URS is canonical. Out-of-URS features must be added by editing `urs/main.md` and re-running `/foundation:urs` first.

If `--from-urs` not provided, set mode = `discover-derived` and proceed with Step 1 as written.
```

- [ ] **Step 5: Mark Step 2 conditional**

In Step 2, change the opening sentence from:

```
From the use cases in `product-mission.md`, derive the list of features needed.
```

to:

```
**(discover-derived mode only — skip in `--from-urs` mode.)**

From the use cases in `product-mission.md`, derive the list of features needed.
```

- [ ] **Step 6: Add a note to Step 5 (Write Project State)**

In the `## Step 5 — Write Project State` section, append after the markdown table example:

```markdown

**`--from-urs` mode:** include the `URS Ref`, `Zone`, and `Last Updated` columns shown in the existing `project-state.md` template (see ADR 0001). One backlog row per FR row in `urs/index.json`. Maturity starts at `🔲 Pending`. `Spec` column starts empty.
```

- [ ] **Step 7: Update `## ✅ What's Next`**

Append at the end of the closing block:

```markdown

**`--from-urs` next steps:** after the backlog is written, run `/foundation:sprint-plan` to derive a delivery timeline grouped into sprints with Sprint 0 walking skeleton.
```

- [ ] **Step 8: Verify**

Run: `grep -c -- '--from-urs' .claude/commands/foundation/plan.md`
Expected: `≥ 6` mentions.

- [ ] **Step 9: Commit**

```bash
git add .claude/commands/foundation/plan.md
git commit -m "feat(foundation): add --from-urs mode to /foundation:plan"
```

---

## Phase 4 — Sprint Planning Artifact

### Task 8: Write ADR 0003 — URS-First Ingestion + Sprint Planning

ADR locks the four locked decisions from the design discussion: complexity-points sizing, lazy task expansion, full-text `index.json`, deferred subagent fan-out.

**Files:**
- Create: `.claude/docs/adr/0003-urs-first-ingestion-and-sprint-planning.md`

- [ ] **Step 1: Write the ADR**

Create the file with this content exactly:

```markdown
# ADR 0003 — URS-first ingestion and sprint planning

**Status:** Accepted
**Date:** 2026-05-04

---

## Context

ADR 0001 + 0002 established that the factory can compile a URS into
`urs/index.json` and reconcile it into `project-state.md`. The default
factory flow still authors the URS in parallel with discover/plan rather
than starting from a finished URS.

We want a mode where a published URS — possibly hundreds of pages — is the
canonical input, and the factory derives both the backlog and a sprint
delivery timeline from it. Two concerns drive the design:

1. **Token budget.** A 400+ page URS cannot be loaded into LLM context
   in full. Stages must operate on compact derived artifacts, not the
   markdown source.
2. **Cost of speculation.** Eagerly expanding every FR into a task list at
   plan time wastes effort on FRs that may be cut, deferred, or specced
   later. URS edits would invalidate the entire eager expansion.

## Decision

### 1. Sprint sizing — derived complexity points, not raw FR count

A sprint master would not size by FR count alone (each FR has highly
variable effort) nor by risk zone alone (zone is criticality, not effort).
Compute complexity points per FR from URS structure:

```
points = w1 * tables_touched
       + w2 * applies_to_count        (NFR/UR/VR refs from applies-to.json)
       + w3 * risk_zone               (Z1=3, Z2=2, Z3=1)
       + w4 * dep_count               (FR depends on other FRs)
```

Default weights: `w1=2, w2=1, w3=1, w4=1`. Sprint 0 = walking skeleton
(auth + spine FRs, fixed). Subsequent sprints fill a configurable point
budget (default 13).

### 2. Task expansion — lazy, not eager

Per-FR task lists (`spec → migration → RPC → action → component → tests`)
are generated on demand by `/foundation:shape-spec --from-urs FR-XX`.
Sprint estimation uses the complexity heuristic above; it does not
require actual task counts. A 1000-FR URS yields zero task files until
the SA enters that FR.

### 3. `urs/index.json` — full text on disk, slice-by-ID in consumers

Keep full requirement text in `index.json`. Cost is bounded
(≈ 1 MB even for 1500 FRs at ~700 bytes/req). Consumers must extract by
ID via `jq` or programmatic Read — never load the entire file into LLM
context. `applies-to.json` provides the precomputed cross-cutting map so
consumers never need to re-derive.

### 4. Subagent fan-out — deferred

Compile is a deterministic parser (no LLM, no fan-out needed). Clustering
in `/foundation:sprint-plan` is the only candidate for fan-out, and the
need is unproven without measuring on a real heavy URS. Ship single-
agent v1 with the clustering interface designed as a pure function
(`cluster(reqs[], constraints[]) → clusters[]`). Wire fan-out only when
a 200+ FR URS pegs context.

## Consequences

**Positive:**

- Token budget bounded at every stage; works for arbitrarily large URS.
- Sprint plan reflects effort variance, not just count or criticality.
- Lazy task expansion avoids waste; URS edits invalidate at most the
  affected FR's task file.
- Single-source-of-truth for cross-cutting constraints
  (`applies-to.json`).

**Negative / accepted trade-offs:**

- Sprint plan estimates without seeing actual tasks — relies on the
  complexity heuristic. Mitigated by re-running `/foundation:sprint-plan`
  as FRs ship and actual table counts firm up.
- Default weights `w1..w4` are calibrated on a small sample. Re-tune
  after running on 5+ real URS projects.
- `applies-to.json` derivation is regex-based (`FR-\d{2,3}` mention
  scan). Misses cases where a NFR/UR row implicitly applies without
  naming an FR. Mitigated by warning when `applies_to` resolves to
  `["*"]`.

**Out of scope:**

- Per-sprint capacity calibration from historical velocity (no history
  to draw on yet — re-evaluate after 3+ projects).
- Fan-out implementation (deferred per decision 4).
```

- [ ] **Step 2: Verify**

Run: `wc -l .claude/docs/adr/0003-urs-first-ingestion-and-sprint-planning.md && head -5 .claude/docs/adr/0003-urs-first-ingestion-and-sprint-planning.md`
Expected: `≥ 80` lines; first line `# ADR 0003 — URS-first ingestion and sprint planning`.

- [ ] **Step 3: Commit**

```bash
git add .claude/docs/adr/0003-urs-first-ingestion-and-sprint-planning.md
git commit -m "docs: ADR 0003 for URS-first ingestion and sprint planning"
```

### Task 9: Create `/foundation:sprint-plan` command

**Files:**
- Create: `.claude/commands/foundation/sprint-plan.md`

- [ ] **Step 1: Write the command spec**

Create the file with this content exactly:

```markdown
# /foundation:sprint-plan

Derive a sprint delivery timeline from a compiled URS. Reads
`urs/index.json` + `urs/applies-to.json`, computes complexity points per
FR, builds the dependency DAG, groups FRs into delivery waves, and emits
`urs/clusters.json` + `urs/sprint-plan.md`.

This is **not authoring** — the URS already declared the requirements.
This is **scheduling** — given the requirements, what should ship first
and in what order.

**Usage:**

- `/foundation:sprint-plan` — generate the timeline using the default
  point budget (13 per sprint after Sprint 0).
- `/foundation:sprint-plan --budget N` — override the per-sprint
  point budget.

**Preconditions:**

- `urs/index.json` must exist (run `/foundation:urs`).
- `urs/applies-to.json` must exist (emitted alongside `index.json` —
  re-run `/foundation:urs` if missing).
- `.claude/docs/project-state.md` should already have the URS-derived
  backlog rows (run `/foundation:plan --from-urs` first).

Read before starting:

- `urs/index.json` — slice by ID; do **not** load the whole file into
  LLM context if the FR count exceeds 100. Use `jq` or programmatic
  Read with offset/limit.
- `urs/applies-to.json` — full file (always small).
- `.claude/project-config.json` — multi-tenant flag affects walking
  skeleton.
- `.claude/docs/project-state.md` — current backlog state.
- `.claude/docs/adr/0003-urs-first-ingestion-and-sprint-planning.md` —
  binding design decisions for this command.

---

## Step 1 — Load Compact Inputs

1. Read `urs/applies-to.json` in full.
2. Read `urs/index.json` and extract a **compact projection** with only
   these fields per FR row:
   - `id`, `title`, `risk_zone`, `section_anchor`
   - `text` truncated to 200 chars (full text not needed for planning)
3. Build an in-memory list `frs[]` of compact rows. NFR/UR/VR rows are
   not needed here — `applies-to.json` already summarized them.

If the URS has more than 200 FRs and the compact projection still
exceeds 50 KB after truncation, dispatch a subagent per
`section_anchor` to compute clusters in parallel and merge the results.
(Single-agent v1 path is the default; the fan-out hook is described in
ADR 0003 and is out of scope for this command's first implementation —
emit a `// TODO: fan-out` marker if the threshold is hit.)

---

## Step 2 — Derive Complexity Points

For each FR, compute:

```
tables_touched   ≈ 1 + (count of FR-id co-references in this FR's text)
applies_to_count = applies_to.by_fr[FR.id].length
risk_weight      = 4 - risk_zone     (Z1=3, Z2=2, Z3=1)
dep_count        = number of other FRs this FR depends on (see Step 3)

points = 2 * tables_touched
       + 1 * applies_to_count
       + 1 * risk_weight
       + 1 * dep_count
```

If a project-config.json override exists at
`projectConfig.sprintPlan.weights = { w1, w2, w3, w4 }`, use those
instead of the defaults.

Round to the nearest integer. Minimum points per FR = 1.

---

## Step 3 — Build the Dependency DAG

Two FRs are dependent if **either**:

1. FR-A's text mentions FR-B's id (`FR-04` named in FR-05's text).
2. FR-A's `section_anchor` is a strict superset path of FR-B's anchor
   (e.g. `approval/decide` depends on `approval/queue`). Use the URS
   author's section ordering as a soft hint — same section + earlier id
   → likely dependency.

Output a `dependencies` map `{ FR-id → [other FR-ids] }`.

If the resulting graph has a cycle, abort with a clear message naming
the cycle members and ask the SA to resolve in `urs/main.md` before
re-running.

---

## Step 4 — Cluster

A cluster is a set of FRs that share enough surface area to ship
together (shared schema, shared persona, shared UI surface). Heuristic:

- Group by `section_anchor` first.
- Within a section, split if the dependency depth between two FRs > 1.
- A cluster's risk zone = max risk zone of its members.
- A cluster's points = sum of member points.

Emit clusters in topological order (no cluster depends on a later one).

---

## Step 5 — Sprint 0: Walking Skeleton

Sprint 0 is fixed and never sized by points. It contains:

- Auth scaffolding (covered by `/foundation:init` baseline — no FR
  needed unless the URS specifies extra auth requirements).
- Tenant onboarding if `multiTenant: true`.
- The single highest-priority FR from the first cluster (by risk zone,
  tiebreaker = lowest id), specced to walking-skeleton depth: happy-
  path only, no edge cases. This proves the schema spine end-to-end.

Sprint 0 has no point budget. Estimated 1 sprint of work regardless.

---

## Step 6 — Pack Sprints

Greedy bin-packing across remaining clusters in topological order:

- Open Sprint 1 with budget = config.budget (default 13 points).
- Add the first available cluster whose dependencies are all in Sprint 0
  or earlier sprints. If the cluster fits the remaining budget, place
  it. Otherwise close the current sprint and open the next.
- Repeat until all FRs are placed.

If a single cluster exceeds the budget, place it in its own sprint and
note "oversized" in the sprint-plan.md output. The SA can then split
the cluster manually in `urs/main.md` (rename FR ids if needed) and
re-run.

---

## Step 7 — Emit `urs/clusters.json`

```json
{
  "compiled_at": "<ISO 8601>",
  "weights": { "w1": 2, "w2": 1, "w3": 1, "w4": 1 },
  "budget_per_sprint": 13,
  "dependencies": { "FR-05": ["FR-04"], "FR-06": ["FR-05"] },
  "clusters": [
    {
      "id": "cluster-1",
      "section_anchor": "registration",
      "frs": ["FR-01", "FR-02", "FR-03"],
      "risk_zone": 1,
      "points": 12
    }
  ]
}
```

---

## Step 8 — Emit `urs/sprint-plan.md`

Human-readable plan. Format:

```markdown
# Sprint Plan — <project_name> v<version>

Compiled: <timestamp> by /foundation:sprint-plan
Budget per sprint: <N> points (defaults from ADR 0003)

## Sprint 0 — Walking Skeleton

Goal: prove schema spine end-to-end.

| FR | Title | Risk Zone | Points |
|----|-------|:--------:|:------:|
| FR-01 | Submit registration | 1 | (skeleton-depth) |

Notes: auth scaffolding from `/foundation:init` baseline.

## Sprint 1

| FR | Title | Cluster | Zone | Points | Depends On |
|----|-------|---------|:----:|:------:|------------|
| FR-02 | Validate NIK format | registration | 1 | 4 | FR-01 |
| FR-03 | View own status | registration | 1 | 3 | FR-01 |
| FR-04 | Regional review queue | approval | 1 | 6 | FR-01 |

Sprint 1 total: 13 points.

(repeat per sprint)

## Risks and Notes

- Oversized clusters: <list any>
- Cycles detected: <none, or list>
- `applies_to: ["*"]` warnings carried over from compile: <count>
```

Write the file. Do not overwrite a hand-edited section if it begins with
a `<!-- editor: keep -->` HTML comment — preserve that block verbatim
and append everything else.

---

## Step 9 — Reconcile `project-state.md`

For each FR, write a `Sprint` column entry indicating its sprint number.
If the column does not exist yet, add it after `Zone`. Do not overwrite
existing maturity values.

---

## Step 10 — Summary Output

Print:

```
Sprint Plan — <project_name> v<version>
Sprints: N
Total FRs: M
Total points (excl. Sprint 0): P
Oversized clusters: K
Cycles: 0 | <list>

Files written:
  urs/clusters.json
  urs/sprint-plan.md
  .claude/docs/project-state.md (modified — Sprint column)

Warnings: <if any>
```

---

## ✅ What's Next

Tell the user:

"Sprint plan written. Start Sprint 0 by running
`/foundation:shape-spec --from-urs <highest-priority-FR>` for the
walking-skeleton FR. After Sprint 0 ships, return here and re-run
`/foundation:sprint-plan` to refine subsequent sprints from observed
velocity."

```
COMMAND_COMPLETE: foundation:sprint-plan
STATUS: success
FILES_CREATED: urs/clusters.json, urs/sprint-plan.md
FILES_MODIFIED: .claude/docs/project-state.md
NEXT_COMMAND: /foundation:shape-spec --from-urs <FR-XX>
```
```

- [ ] **Step 2: Verify**

Run: `grep -c '^## Step' .claude/commands/foundation/sprint-plan.md`
Expected: `≥ 10`.

Run: `grep -c 'COMMAND_COMPLETE: foundation:sprint-plan' .claude/commands/foundation/sprint-plan.md`
Expected: `1`.

- [ ] **Step 3: Commit**

```bash
git add .claude/commands/foundation/sprint-plan.md
git commit -m "feat(foundation): add /foundation:sprint-plan command"
```

---

## Phase 5 — Lazy Task Expansion

### Task 10: Update `/foundation:shape-spec` to write `urs/tasks/FR-XX.json`

When invoked with `--from-urs FR-XX`, after the spec is finalized, also write the per-FR task breakdown so subsequent commands (`architecture:new-feature`, etc.) can read a deterministic task list rather than re-deriving one.

**Files:**
- Modify: `.claude/commands/foundation/shape-spec.md`

- [ ] **Step 1: Read current Step 0 and final-output section**

Run: `grep -n '^## Step\|^## ✅\|^## Step 0' .claude/commands/foundation/shape-spec.md`

- [ ] **Step 2: Extend Step 0 with `applies-to.json` injection**

In the existing `## Step 0 — Optional URS Lookup` section, after item 3 ("Pre-fill spec context from the URS row"), add a new sub-step:

```markdown
4a. Read `urs/applies-to.json` (if present) and look up
    `by_fr["<FR-XX>"]`. For every constraint id (NFR/UR/VR), fetch its
    full text from `urs/index.json` and add it to the spec's
    front-matter under `constraints:` so `/architecture:new-feature` and
    `/qa:new-tests` see them without re-deriving.
```

Renumber the existing item 4 to item 5.

- [ ] **Step 3: Add a final Step 6 — Emit task breakdown (when `--from-urs`)**

Insert a new step at the end (before `## ✅ What's Next`):

```markdown
## Step 6 — Emit per-FR Task Breakdown (`--from-urs` only)

After the spec markdown is written, also emit `urs/tasks/<FR-id>.json`
to record the canonical task list for downstream commands. This is
**lazy expansion** — only the FR currently being specced gets a task
file; other FRs do not.

```json
{
  "fr_id": "FR-03",
  "spec_path": ".claude/docs/specs/<feature-slug>.md",
  "constraints": ["UR-01", "VR-01"],
  "tasks": [
    { "stage": "spec",            "status": "completed", "owner": "/foundation:shape-spec" },
    { "stage": "architecture",    "status": "pending",   "owner": "/architecture:new-feature" },
    { "stage": "implementation",  "status": "pending",   "owner": "/implementation:new-feature" },
    { "stage": "tests",           "status": "pending",   "owner": "/qa:new-tests" },
    { "stage": "review",          "status": "pending",   "owner": "/architecture:review or /implementation:review" },
    { "stage": "release",         "status": "pending",   "owner": "/deployment:release" }
  ],
  "created_at": "<ISO 8601>",
  "updated_at": "<ISO 8601>"
}
```

Create `urs/tasks/` directory if missing. The file is updated by each
downstream command as the FR progresses through the maturity pipeline.

If the file already exists (re-spec), preserve every task whose
`status` is `completed`; reset only `pending` and `in_progress` rows
to match the new spec.
```

- [ ] **Step 4: Update closing block**

Find the closing `COMMAND_COMPLETE: foundation:shape-spec` block. Update `FILES_CREATED:` to add (conditionally, when `--from-urs` mode):

```
FILES_CREATED: .claude/docs/specs/<feature-slug>.md[, urs/tasks/<FR-id>.json when --from-urs]
```

- [ ] **Step 5: Verify**

Run: `grep -c 'urs/tasks' .claude/commands/foundation/shape-spec.md`
Expected: `≥ 3`.

- [ ] **Step 6: Commit**

```bash
git add .claude/commands/foundation/shape-spec.md
git commit -m "feat(foundation): lazy task expansion — write urs/tasks/FR-XX.json from shape-spec"
```

---

## Phase 6 — Validation + Documentation

### Task 11: Update `/foundation:validate` to check URS-first artifacts

**Files:**
- Modify: `.claude/commands/foundation/validate.md`

- [ ] **Step 1: Read existing structure**

Run: `grep -n '^## Step' .claude/commands/foundation/validate.md`

- [ ] **Step 2: Add a URS-first artifacts step**

Insert a new section between the existing `## Step 4 — Check Feature Files` and the next step (or at the end of step 4 if it's last):

```markdown
## Step 4.5 — URS-First Artifacts (when `urs/main.md` exists)

If `urs/main.md` is present, the project is using URS-first ingestion.
Verify the compile artifacts and downstream consumers are present and
consistent.

| File | Required When | Created By |
|------|---------------|------------|
| `urs/main.md` | URS authored | `/foundation:urs:draft` (or hand-authored) |
| `urs/main.tex` | URS compiled | `/foundation:urs` |
| `urs/index.json` | URS compiled | `/foundation:urs` |
| `urs/applies-to.json` | URS compiled | `/foundation:urs` (Step 4.5) |
| `urs/clusters.json` | Sprint planned | `/foundation:sprint-plan` |
| `urs/sprint-plan.md` | Sprint planned | `/foundation:sprint-plan` |
| `urs/tasks/FR-XX.json` | FR specced | `/foundation:shape-spec --from-urs` |

Cross-checks:

1. Every constraint in `urs/applies-to.json` references an existing FR
   id from `urs/index.json`. Report any orphans.
2. Every FR id in `urs/index.json` appears as a row in
   `project-state.md` with `urs_ref = <FR-id>`. Report missing rows.
3. Every `urs/tasks/FR-XX.json` corresponds to an FR id present in
   `urs/index.json`. Report orphan task files.
4. `compiled_at` timestamps in `index.json` and `applies-to.json` match
   (they were written together). Mismatch → re-run `/foundation:urs`.

If `urs/main.md` is absent, skip this step entirely (project is using
the discover-derived flow — that is fine).
```

- [ ] **Step 3: Verify**

Run: `grep -c '^## Step 4.5' .claude/commands/foundation/validate.md && grep -c 'applies-to.json' .claude/commands/foundation/validate.md`
Expected: first = `1`, second `≥ 2`.

- [ ] **Step 4: Commit**

```bash
git add .claude/commands/foundation/validate.md
git commit -m "feat(foundation): /foundation:validate checks URS-first artifacts"
```

### Task 12: Update CLAUDE.md (workflow + commands table)

**Files:**
- Modify: `.claude/CLAUDE.md`

- [ ] **Step 1: Locate the Development Workflow code block and the All Commands table**

Run: `grep -n '## Development Workflow\|## All Commands\|## Non-Negotiable Rules' .claude/CLAUDE.md`

- [ ] **Step 2: Add a URS-first lane to the workflow code block**

In the `## Development Workflow` section, the existing fenced block has a `── Setup ──` lane. Insert a new lane between it and the per-feature lane:

```text
── URS-first ingestion (alternative to discover) ────────
/foundation:urs:draft   → AI-assisted URS authoring → urs/main.md
/foundation:urs         → compile URS → urs/index.json + applies-to.json
/foundation:plan --from-urs   → backlog from URS FR rows
/foundation:sprint-plan → cluster + sprint timeline → urs/sprint-plan.md
```

- [ ] **Step 3: Insert rows into the All Commands table**

Add three new rows after the existing `/foundation:plan` row:

```markdown
| `/foundation:urs:draft`        | Author URS Markdown from a product brief                              |
| `/foundation:urs`              | Compile URS Markdown → LaTeX + index.json + applies-to.json           |
| `/foundation:sprint-plan`      | Cluster URS FRs + emit sprint timeline (Sprint 0 walking skeleton)    |
```

Also update the existing `/foundation:plan` row description from:

```
| `/foundation:plan`             | Plan all features, create backlog in project-state.md                 |
```

to:

```
| `/foundation:plan`             | Plan features → backlog (`--from-urs` to ingest a published URS)      |
```

- [ ] **Step 4: Add ADR 0003 reference under "Finding the Right Standards"**

Locate the bulleted list of "Key documents:" and append:

```markdown
- `.claude/docs/adr/0001-urs-project-state-separation.md` — URS / project-state separation
- `.claude/docs/adr/0002-urs-source-format.md` — Markdown→LaTeX URS source format
- `.claude/docs/adr/0003-urs-first-ingestion-and-sprint-planning.md` — URS-first mode and sprint sizing rules
```

- [ ] **Step 5: Verify**

Run: `grep -c 'sprint-plan\|urs:draft\|--from-urs' .claude/CLAUDE.md`
Expected: `≥ 5`.

- [ ] **Step 6: Commit**

```bash
git add .claude/CLAUDE.md
git commit -m "docs(claude-md): add URS-first workflow and commands"
```

### Task 13: Update `.claude/docs/workflows.md` with a URS-first workflow

**Files:**
- Modify: `.claude/docs/workflows.md`

- [ ] **Step 1: Read the existing workflows.md table of contents**

Run: `grep -n '^## ' .claude/docs/workflows.md`

- [ ] **Step 2: Append a new section at the end**

Append:

```markdown

---

## URS-First Development Workflow

Use this lane when the project starts from a finished, published URS — for
example, regulated procurements where the Software Architect receives a
signed requirements document and must derive the build plan from it.

### 1. Drop in the URS

Place the URS Markdown at `urs/main.md`. It must follow the structure in
ADR 0002 (front-matter + section headings + requirement tables). If you
do not have a Markdown URS yet, run `/foundation:urs:draft` first to
author one from a product brief.

### 2. Compile

```
/foundation:urs
```

This emits:
- `urs/main.tex` — formal LaTeX artifact (signable)
- `urs/index.json` — machine-readable contract
- `urs/applies-to.json` — NFR/UR/VR → FR cross-cutting map
- updates `.claude/docs/project-state.md` with FR-derived backlog rows

### 3. Plan from URS

```
/foundation:plan --from-urs
```

Skips the discover-derived feature derivation. Reads `urs/index.json` and
writes the FR rows directly into the backlog with risk zones and
dependencies.

### 4. Sprint plan

```
/foundation:sprint-plan
```

Computes complexity points per FR, builds the dependency DAG, groups FRs
into clusters, packs them into sprints with Sprint 0 as a walking
skeleton. Emits `urs/clusters.json` + `urs/sprint-plan.md`.

### 5. Per-FR build (lazy)

For each FR in Sprint 0, then Sprint 1, etc.:

```
/foundation:shape-spec --from-urs FR-XX
/architecture:new-feature
/implementation:new-feature
/qa:new-tests
/qa:fix
```

Each `shape-spec --from-urs` writes `urs/tasks/FR-XX.json` so downstream
commands have a deterministic task list.

### 6. Validate

```
/foundation:validate
```

Verifies every URS FR has a backlog row, every constraint references a
real FR, and `index.json`/`applies-to.json` timestamps match.

### 7. Release

```
/deployment:release
```

For regulated projects, the release gate cross-checks every shipped FR
against its URS row and flags any drift.

---

### Token-budget notes for very large URS

- `urs/index.json` may exceed 1 MB on a 1500+ FR document. Never load
  the whole file into LLM context — extract by ID via `jq` or
  programmatic Read.
- `/foundation:sprint-plan` operates on a compact projection (≤ 200 char
  per FR), bounded at ~50 KB even for 1000 FRs.
- Per-FR work uses only the relevant FR row + its constraints from
  `applies-to.json` — typically < 2 KB context.

See ADR 0003 for the full design rationale.
```

- [ ] **Step 3: Verify**

Run: `grep -c '^## URS-First' .claude/docs/workflows.md`
Expected: `1`.

- [ ] **Step 4: Commit**

```bash
git add .claude/docs/workflows.md
git commit -m "docs(workflows): add URS-first development workflow"
```

### Task 14: Update README.md with URS-first phase sequence

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Read the current "Follow the guided sequence" section**

Run: `grep -n 'guided sequence\|Phase Sequence\|Credits' README.md`

- [ ] **Step 2: Add an alternative sequence diagram**

Locate the existing diagram block that starts with `/foundation:init` and ends before `## Phase Sequence`. Immediately after that block, insert:

```markdown

### Alternative: URS-first sequence

If you start from a published URS instead of running `/foundation:discover`:

```
/foundation:init
    ↓
/foundation:urs:draft        (or drop urs/main.md in directly)
    ↓
/foundation:urs              (compile → index.json + applies-to.json)
    ↓
/foundation:plan --from-urs  (backlog from FR rows)
    ↓
/foundation:sprint-plan      (cluster + Sprint 0 walking skeleton + waves)
    ↓
── per FR (one session each) ────────────────────────────
/foundation:shape-spec --from-urs FR-XX
    ↓
/architecture:new-feature
    ↓
/implementation:new-feature
    ↓
/qa:new-tests → /qa:fix
─────────────────────────────────────────────────────────
    ↓
/deployment:release
```

See `.claude/docs/workflows.md` for the full URS-first walkthrough and
`.claude/docs/adr/0003-urs-first-ingestion-and-sprint-planning.md` for
the design decisions.
```

- [ ] **Step 3: Verify**

Run: `grep -c 'URS-first\|--from-urs' README.md`
Expected: `≥ 4`.

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs(readme): add URS-first phase sequence"
```

### Task 15: Update standards-index.yml

**Files:**
- Modify: `.claude/docs/standards-index.yml`

- [ ] **Step 1: Read current entries**

Run: `cat .claude/docs/standards-index.yml`

- [ ] **Step 2: Add ADR entries**

Add three entries (location depends on the existing structure — append to a `decisions:` or `adr:` section if one exists; otherwise add a new section near the top):

```yaml
adr:
  - id: ADR-0001
    title: URS and project-state.md are separate concerns
    path: .claude/docs/adr/0001-urs-project-state-separation.md
    topics: [urs, project-state, traceability, regulated]
  - id: ADR-0002
    title: URS source is Markdown, compiled to LaTeX
    path: .claude/docs/adr/0002-urs-source-format.md
    topics: [urs, source-format, latex, compilation]
  - id: ADR-0003
    title: URS-first ingestion and sprint planning
    path: .claude/docs/adr/0003-urs-first-ingestion-and-sprint-planning.md
    topics: [urs, sprint-planning, complexity-points, lazy-expansion, token-budget]
```

If a `decisions:`/`adr:` key already exists in the file, merge into it instead of duplicating.

- [ ] **Step 3: Verify**

Run: `grep -c 'ADR-000[123]' .claude/docs/standards-index.yml`
Expected: `3`.

- [ ] **Step 4: Commit**

```bash
git add .claude/docs/standards-index.yml
git commit -m "docs(standards-index): register ADR 0001-0003"
```

---

## Phase 7 — Smoke Test

### Task 16: End-to-end smoke test against SIMREG URS

Use the existing SIMREG URS source from the `xlsmart-coaching` branch as test input. Confirm the new pipeline runs cleanly on `main`.

**Files:**
- (no source changes — verification only)

- [ ] **Step 1: Copy SIMREG URS source to main as a temporary test fixture**

```bash
git show xlsmart-coaching:urs/main.md > /tmp/simreg-urs.md
mkdir -p urs
cp /tmp/simreg-urs.md urs/main.md
```

- [ ] **Step 2: Run `/foundation:urs` and confirm artifact set**

Invoke: `/foundation:urs`
Expected output files: `urs/main.tex`, `urs/index.json`, `urs/applies-to.json`.
Expected: drift report shows N FRs added to backlog, 0 orphans, 0 cycles.

- [ ] **Step 3: Confirm `applies-to.json` shape**

```bash
jq '.by_fr | keys | length' urs/applies-to.json
```

Expected: equals number of FR rows in `index.json`.

- [ ] **Step 4: Run `/foundation:plan --from-urs`**

Invoke: `/foundation:plan --from-urs`
Expected: `project-state.md` backlog populated with FR rows. No prompt to derive features.

- [ ] **Step 5: Run `/foundation:sprint-plan`**

Invoke: `/foundation:sprint-plan`
Expected files: `urs/clusters.json`, `urs/sprint-plan.md`.
Expected: Sprint 0 contains 1 walking-skeleton FR. Sprint 1+ totals roughly 13 points each.

- [ ] **Step 6: Run `/foundation:shape-spec --from-urs FR-01`**

Invoke: `/foundation:shape-spec --from-urs FR-01`
Expected: spec file written under `.claude/docs/specs/`. Expected file: `urs/tasks/FR-01.json` with the 6-task array, only `spec` marked completed.

- [ ] **Step 7: Run `/foundation:validate`**

Invoke: `/foundation:validate`
Expected: URS-first artifacts section reports all green, `compiled_at` matches, no orphans.

- [ ] **Step 8: Roll back the smoke-test fixtures**

The smoke test brought in SIMREG-specific files. They are demo data, not factory baseline — they belong on `xlsmart-coaching`, not `main`.

```bash
git checkout -- urs/      # discard generated index.json, applies-to.json, etc.
rm -rf urs/                # remove untracked smoke-test outputs
git status --short        # verify clean
```

If `git status` still shows files under `urs/`, list them and confirm with the user before removing.

- [ ] **Step 9: Final smoke-test summary commit (optional)**

If you authored a short test report file at `.claude/docs/plans/2026-05-04-urs-first-smoke-test-results.md`, commit it now. Otherwise no commit.

---

## Self-Review Checklist

Before declaring this plan complete:

- [ ] Every Phase 1 cherry-pick verified to apply cleanly on `main`. If any conflict, the plan must add a manual-merge sub-step.
- [ ] Every modification step shows the exact text to insert, not a description.
- [ ] Every command spec contains its full `## Step N` skeleton, not "see existing pattern".
- [ ] Every ADR is dated `2026-05-04` and signed.
- [ ] Every commit message follows the repo's conventional commit style (`feat(foundation):`, `docs(claude-md):`, `chore:`).
- [ ] No step references a function, file, or flag not defined in this plan or the cherry-picked baseline.
